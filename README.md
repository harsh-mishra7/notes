# Notes

Personal study notes. One folder per subject, one Markdown file per topic.

## Index

<!-- Add a line here when you start a new subject -->

- _(nothing yet — create a folder and start writing)_

## Conventions

- Folder per subject: `javascript/`, `system-design/`, `dsa/`
- File per topic, kebab-case: `event-loop.md`, `rate-limiting.md`
- Images go in `assets/` next to the note that uses them

## Workflow

```bash
# write notes, then:
git add .
git commit -m "notes: <topic>"
git push
```
